Imports System.Text

Public Class FrmTools

    Private Sub Button1_Click(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles Button1.Click
        'Dim path As String = "C:/Users/zhangm/Desktop/GJDQ.txt"

        'Dim txt As String = My.Computer.FileSystem.ReadAllText(path, Encoding.UTF8)
        'Dim lines As String() = txt.Split(vbNewLine)
        'Dim cols As String()
        'Dim buf As New StringBuilder

        'For i As Integer = 0 To lines.Length - 1
        '    Dim line As String = Trim(lines(i).Replace(vbLf, ""))
        '    If "".Equals(line) OrElse IsNumeric(line) Then
        '        Continue For
        '    End If

        '    buf.AppendLine(line)
        'Next

        'My.Computer.FileSystem.WriteAllText(path, buf.ToString, False, Encoding.UTF8)
        'Me.Close()

    End Sub
End Class
